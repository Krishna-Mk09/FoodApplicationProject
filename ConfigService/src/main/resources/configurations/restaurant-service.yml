# ====================================================
# RestaurantService - Service-Specific Config
# ====================================================
server:
  port: 8085

spring:
  application:
    name: restaurant-service
  kafka:
    producer:
      properties:
        enable.idempotence: true
      acks: all
      retries: 3
