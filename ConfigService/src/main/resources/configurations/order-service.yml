# ====================================================
# OrderService - Service-Specific Config
# ====================================================
server:
  port: 8089

spring:
  application:
    name: order-service
  cloud:
    compatibility-verifier:
      enabled: true
  servlet:
    multipart:
      enabled: true
      max-file-size: 10MB
      max-request-size: 10MB
